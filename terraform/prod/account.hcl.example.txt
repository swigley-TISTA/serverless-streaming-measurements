# Set account-wide variables. These are automatically pulled in to configure the remote state bucket in the root
# terragrunt.hcl configuration.
locals {
  account_name   = "my-aws-account"
  aws_account_id = "aws-account-id-1234"
  aws_profile    = "dev"
}
